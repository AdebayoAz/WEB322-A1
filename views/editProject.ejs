<!DOCTYPE html>
<html lang="en" data-theme="coffee">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Project - Climate Solutions</title>
    <link rel="stylesheet" href="/css/main.css">
</head>
<body>
    <%- include('partials/navbar') %>

    <div class="container mx-auto p-6">
      <!-- Hero -->
      <div class="hero bg-base-200 rounded-lg p-8 mb-6">
        <div class="hero-content">
          <div>
            <h1 class="text-4xl font-bold">Edit Project</h1>
            <p class="py-3">Edit Project: <%= project.title %></p>
          </div>
        </div>
      </div>

      <!-- Form -->
      <div class="card bg-base-100 shadow-lg">
        <div class="card-body">
          <form method="POST" action="/solutions/editProject">
            <input type="hidden" name="id" value="<%= project.id %>">
            
            <div class="form-control mb-4">
              <label class="label">
                <span class="label-text">Title</span>
              </label>
              <input type="text" name="title" value="<%= project.title %>" placeholder="Project Title" class="input input-bordered" required>
            </div>

            <div class="form-control mb-4">
              <label class="label">
                <span class="label-text">Feature Image URL</span>
              </label>
              <input type="url" name="feature_img_url" value="<%= project.feature_img_url %>" placeholder="https://example.com/image.jpg" class="input input-bordered" required>
            </div>

            <div class="form-control mb-4">
              <label class="label">
                <span class="label-text">Sector</span>
              </label>
              <select name="sector_id" class="select select-bordered" required>
                <option value="1" <%= (project.sector_id == 1) ? "selected" : "" %>>Land Sinks</option>
                <option value="2" <%= (project.sector_id == 2) ? "selected" : "" %>>Industry</option>
                <option value="3" <%= (project.sector_id == 3) ? "selected" : "" %>>Transportation</option>
                <option value="4" <%= (project.sector_id == 4) ? "selected" : "" %>>Electricity</option>
                <option value="5" <%= (project.sector_id == 5) ? "selected" : "" %>>Food, Agriculture, and Land Use</option>
              </select>
            </div>

            <div class="form-control mb-4">
              <label class="label">
                <span class="label-text">Introduction</span>
              </label>
              <textarea name="intro_short" class="textarea textarea-bordered" placeholder="Brief introduction..." required><%= project.intro_short %></textarea>
            </div>

            <div class="form-control mb-4">
              <label class="label">
                <span class="label-text">Summary</span>
              </label>
              <textarea name="summary_short" class="textarea textarea-bordered" placeholder="Short summary..." required><%= project.summary_short %></textarea>
            </div>

            <div class="form-control mb-4">
              <label class="label">
                <span class="label-text">Impact</span>
              </label>
              <textarea name="impact" class="textarea textarea-bordered" placeholder="Impact description..." required><%= project.impact %></textarea>
            </div>

            <div class="form-control mb-4">
              <label class="label">
                <span class="label-text">Original Source URL</span>
              </label>
              <input type="url" name="original_source_url" value="<%= project.original_source_url %>" placeholder="https://example.com/source" class="input input-bordered" required>
            </div>

            <div class="form-control mt-6">
              <button type="submit" class="btn btn-success">Update Project</button>
              <a href="/solutions/projects/<%= project.id %>" class="btn btn-secondary mt-2">Cancel</a>
              <a href="/solutions/deleteProject/<%= project.id %>" class="btn btn-error mt-2" onclick="return confirm('Are you sure you want to delete this project?');">Delete Project</a>
            </div>
          </form>
        </div>
      </div>
    </div>
</body>
</html>

